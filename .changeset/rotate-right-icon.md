---
"@zerodev/react-ui": patch
---

feat: add `rotate-right` icon to the shared icon set. Available via `<Icon name="rotateRight" />`; used by the SRA widget's `ErrorRetryCard` to signal a retry affordance on address-creation / route / polling failures.
